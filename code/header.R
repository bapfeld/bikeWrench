# header file for project
setwd("~/strava")

suppressMessages(library(dplyr))
suppressMessages(library(ggplot2))
library(tidyr)
library(strava)

print("Packages loaded: dplyr, tidyr, ggplot2, strava")
