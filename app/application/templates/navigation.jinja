<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a class="navbar-brand">Bike Wrench</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                      <a class="nav-link"
                         href="{{ url_for('index') }}">
                         Home
                      </a>
                    </li>
                    <!-- Rider dropdown menu -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true"
                           aria-expanded="false">Rider</a>
                        <div class="dropdown-menu">
                          <a class="dropdown-item"
                             href="{{ url_for('rider') }}">
                             Rider Info
                          </a>
                          <a class="dropdown-item"
                             href="{{ url_for('edit_rider') }}">
                             Edit Rider
                          </a>
                        </div>
                    </li>
                    <!-- Bike dropdown menu -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true"
                           aria-expanded="false">Bikes</a>
                        <div class="dropdown-menu">
                          <a class="dropdown-item"
                             href="{{ url_for('bikes') }}">
                             Bike list
                          </a>
                            <div class="dropdown-divider"></div>
                            {% for b in bike_menu_list %}
                              <a class="dropdown-item"
                                 href="{{ url_for('bike', id=bike_id) }}">
                                 {{ b.name }}
                              </a>
                            {% endfor %}
                        </div>
                    </li>
                    <!-- Parts dropdown menu -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true"
                           aria-expanded="false">Parts</a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="#">Parts by bike</a>
                            <a class="dropdown-item" href="#">Parts by type</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="#">Part stats by part</a>
                            <a class="dropdown-item"
                               href="{{ url_for('part_averages') }}">
                               Part stats by type
                            </a>
                        </div>
                    </li>
                    <!-- Strava dropdown menu -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true"
                           aria-expanded="false">Strava</a>
                        <div class="dropdown-menu">
                          <a class="dropdown-item"
                             href="{{ url_for('fetch_rides') }}">
                             Fetch new rides
                          </a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item"
                               href="{{ url_for('strava_funcs') }}">
                               Strava app credentials
                            </a>
                        </div>
                    </li>
                </ul>
            </div>
</nav>
